#include "utils.h"

char *intToBits(int i, int size) {

	return (char*) malloc(7*sizeof(char));
}

int BitsToInt(char* bits, int size) {

	return 0;
}

char *substr(char* text, int pos, int size) {

	return (char*) malloc(7*sizeof(char));
}
